[project]
name = "mcp-bridge"
version = "0.1.0"
description = "Bridge midcine pipeline to NEXUS-AI agents over MCP"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "pybreaker>=1.2",
    "pyyaml>=6.0",
    "pydantic>=2.9",
    "structlog>=24.4",
]

[tool.uv]
package = false
