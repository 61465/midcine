import { AuthForm } from '../_components/auth/auth-form';

export const metadata = { title: 'midcine — Start free trial' };

export default function SignupPage() {
  return <AuthForm mode="signup" />;
}
