[project]
name = "midcine-dicom"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydicom>=2.4",
    "numpy>=1.26",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["midcine_dicom"]
